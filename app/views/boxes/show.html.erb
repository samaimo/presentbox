<%= render 'shared/header' %>
<!--メイン-->
<main>
  <div class="contents-wrap">
    <div class="left-side">

    </div>
    <div class="center">
      <!--メイン表示-->
      <% if current_user == @box.user %>
        <%= render 'show-ed' %>
      <% end %>
      <%= render 'show-main' %>

    </div>
    <div class="right-side">

    </div>
  </div>
</main>
